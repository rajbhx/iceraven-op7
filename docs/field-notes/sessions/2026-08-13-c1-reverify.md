# Session digest — 2026-08-13 — C1 re-verification (honest media playback proof)

## Problems solved
- **P** user challenged r5 C1 "verified" — never saw a video playing while texting
  cause: r5 evidence was codec enumeration (HardwareCodecCapability) + low CPU; probe clip URLs were 404, so no MediaCodec session ever ran; the "test" never played video
  solution: retracted r5 method; built fixture pages that beacon currentTime to a local 127.0.0.1 server every 2s; proved AVC/HEVC/VP9 play simultaneously with advancing time-series AND live OMX.qcom.video.decoder.* MediaCodec sessions (component_init success, surface gen) in our media pid
  section: D
  tags: media, codecs, c1, verification, honest-benchmark
- **P** fixture video URLs 404 (test-videos.co.uk 30s/10MB variants)
  cause: site paths changed; only 10s/1MB variants still serve 200
  solution: switched fixtures to Big_Buck_Bunny_1080_10s_1MB.{mp4(h264),mp4(h265),webm(vp9)}; verified with curl before trusting
  section: D
  tags: media, fixtures, urls
- **P** every background switch froze the test (no beacons, decoder deinit)
  cause: Gecko pauses muted video in background tabs; foreground ping-pong between browser and chat app made 30s foreground windows impossible
  solution: fixture page unmutes+plays on any tap (autoplay policy satisfied); audible media keeps playing in background, decoder stays alive, agent captures logcat at leisure
  section: D
  tags: media, background, autoplay, energy
- **P** VIEW intent on running Fenix didn't deliver the URL ("Activity not started")
  cause: am start -n on an already-foreground activity only brings the task forward; new tab never fetched (background tabs don't fetch until foregrounded)
  solution: cold start via am force-stop + am start -n with the intent; or chooser resolution (no -n)
  section: D
  tags: intents, fenix, navigation, tabs
- **P** screenshots couldn't reach the agent (proot storage private, shizuku stdout truncates)
  cause: host /sdcard not visible from proot; base64 of 350KB-1.4MB PNGs exceeds ~64K-char per-command shizuku output cap; chunked reassembly lost bytes
  solution: skip screenshots; use uiautomator bounds + beacon logs + user's eyes; documented shizuku transfer cap in field notes
  section: D
  tags: transfer, screenshot, shizuku, constraint

## Outcome
- C1 verdict corrected and now actually proven: GeckoView uses Qualcomm HW decoders (OMX.qcom.video.decoder.avc/hevc/vp9) for all three web codecs; no pref change shipped.
- Evidence: docs/performance/data/20260813-c1-suite/ (beacons + decoder logcat); docs/performance/media.md rewritten.
- Lesson: enumeration ≠ playback. Any "verified" claim must include a live session + advancing time-series + user-visible confirmation.
