# Session digest — 2026-08-13 — C6: startup re-measurement

## Problems solved
- **P** rapid force-stop→start gave Binder Failed transaction + HOT launches
  cause: contention + no settle time; old process survived force-stop
  solution: single commands with force-stop + 3 s + pidof check + LaunchState: COLD verification (D26)
  section: D
  tags: startup, binder, cold-start, measurement
- **P** cold-start numbers looked like a regression (595 → 697 ms)
  cause: contended daily driver, small samples, r2 also measured device-active
  solution: label contended; don't ship startup changes without a clean idle-window baseline
  section: D
  tags: startup, baseline, contention, honest-benchmark

## Outcome
- r4 cold start: 679–731 ms (median ~697) vs r2 575–617 (median 595). Contended; no change justified.
- Stage timeline captured (onCreate → OP7Capabilities → app-startup → profile → onResume → chrome startup finished → MozAfterPaint ~1 s later).
- C6 verdict: no code change; clean-window re-run is the gate before any startup optimization.
